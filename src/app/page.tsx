import Image from "next/image";

export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-between p-24">
      Gist, make static page for now. Use hosting via Network solutions, but
      move DNS over to main church hosting if possible Update to link up Google
      Calendar, static asset (PDF hosting).
      <ul>
        <li>get logo</li>
        <li>get some new pics</li>
        <li>Contentful free plan for files and data</li>
        <li>Google Calendar for calendar</li>
        <li>Static site generation?</li>
      </ul>
    </main>
  );
}
